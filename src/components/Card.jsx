
export function Card({ movie ,feedClicked}) {
  return (
    <li key={movie.userRating} onClick={()=>feedClicked(movie)}>
      <img src={movie.Poster} alt={`${movie.Title} poster`} />
      <h3>{movie.Title}</h3>
      <div>
        <p>
          <span>🗓</span>
          <span>{movie.Year}</span>
        </p>
      </div>
    </li>
  );
}
